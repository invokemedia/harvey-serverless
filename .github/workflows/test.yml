name: Test
on:
  pull_request:
    types: [opened, edited]
    branches:
      - 'main'
jobs:
  run_sls:
    name: SLS
    runs-on: ubuntu-latest
    steps:
      - name: Install
        run: echo "npm install"
      - name: Invoke
        run: echo "sls invoke local -f harvey" #test comment
   